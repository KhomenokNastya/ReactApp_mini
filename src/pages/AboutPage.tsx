import React from 'react'

export function AboutPage() {
    return (
        <div>You are presented with a site with the ability to add new product cards, information about which 
            comes from the server. The title of the message is adjusted in the window that appears when you 
            click on the "+" in the right corner.</div>
    )
}